server:
  port: 8081

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/queue
    username: queue
    password:
    hikari:
      poolName: Hikari
      auto-commit: false